import 'package:test/test.dart';

void main() {
  group('Assemble', () {
    setUpAll(() {});

    tearDownAll(() {});

    setUp(() {});

    tearDown(() {});

    test('output', () {
      // given

      // when

      // then

      expect(1, 0);
    });
  });
}
