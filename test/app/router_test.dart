import 'package:test/test.dart';

void main() {
  group('PreceptRouter', () {
    setUpAll(() {});

    tearDownAll(() {});

    setUp(() {});

    tearDown(() {});

    test('Before and After', () {
      // given

      // when

      // then

      expect(1, 0);
    });
  });
}
