import 'package:test/test.dart';

void main() {
  group('Library', () {
    setUpAll(() {});

    tearDownAll(() {});

    setUp(() {});

    tearDown(() {});

    test('Look up valid part', () {
      // given

      // when
      // library.constructPart();
      // then

      expect(1, 0);
    }, skip: true);
  });
}
