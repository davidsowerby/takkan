name: precept_back4app_client
description: Client-side Precept pacage for use with Back4App
version: 0.0.1
author: David Sowerby
homepage: https://www.preceptblog.co.uk/

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  parse_server_sdk: ^3.1.0
  dio: ^4.0.0
  json_annotation: ^4.4.0
  process_runner: ^4.1.2
  meta: ^1.7.0
  precept_script:
    path: ../precept_script
  precept_backend:
    path: ../precept_backend


dev_dependencies:
  test:
  build_runner:
  mockito: ^5.0.7
  json_serializable: ^6.1.4


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

